services:
  - type: web
    name: seu-nome-do-app
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn BragaMusic.wsgi:application --config gunicorn.conf.py
    envVars:
      - key: SECRET_KEY
        generateValue: true
      - key: DATABASE_URL
        value: "postgresql://neondb_owner:npg_diYvCe3O0oBV@ep-wild-mud-a5rr9e9x-pooler.us-east-2.aws.neon.tech/bragamusic_db?sslmode=require"
      - key: DEBUG
        value: "False"  # ou "True" para depuração